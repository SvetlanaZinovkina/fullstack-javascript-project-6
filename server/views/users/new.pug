extends ../layouts/application

block header
  = t('views.users.new.signUp')

block content
  //+formFor()(action=route('users') method='post')
  //  +input(user, 'firstName', errors, value =t('views.users.firstName'))(type="text")
  //  +input(user, 'lastName', errors, value =t('views.users.lastName'))(type="text")
  //  +input(user, 'email', errors, value =t('views.users.email'))(type="text")
  //  +input(user, 'password', errors, value =t('views.users.password'))(type="password")
  //  +submit(user)(value=t('views.users.new.submit'))

  .card-body.row.p-5
    .col-12.col-md-6.d-flex.align-items-center.justify-content-center
      img.rounded-circle(src="../assets/welcome.png")
    .col-12.col-md-6.mt-3.mt-mb-0
      form(action=route('users'), method="post")
        .form-floating.mb-3
          input.form-control#data_firstName(name="data[firstName]", value="", placeholder=t('views.users.firstName'), type="text")
          label(for="data_firstName")= t('views.users.firstName')
        .form-floating.mb-3
          input.form-control#data_lastName(name="data[lastName]", value="", placeholder=t('views.users.lastName'), type="text")
          label(for="data_lastName")= t('views.users.lastName')
        .form-floating.mb-3
          input.form-control#data_email(name="data[email]", value="", placeholder=t('views.users.email'), type="text")
          label(for="data_email")= t('views.users.email')
        .form-floating.mb-3
          input.form-control#data_password(name="data[password]", value="", placeholder=t('views.users.password'), type="password")
          label(for="data_password")= t('views.users.password')
        input.btn.btn-primary(type="submit", value=t('views.users.new.submit'))
